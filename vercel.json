{
  "version": 2,
  "buildCommand": "npm install && npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run bundle --workspace=apps/backend",
  "builds": [
    {
      "src": "apps/backend/dist/bundle.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/backend/dist/bundle.js"
    }
  ]
}
